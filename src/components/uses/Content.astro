---
import { tool } from "../../data/uses.json";
---

<section>
  <div class="mx-auto max-w-3xl px-8 py-24 sm:py-32">
    <div class="mx-auto max-w-2xl">
      <div>
        <div>
          <div>
            <p
              class="text-2xl font-medium text-balance tracking-tight text-neutral-900 dark:text-white sm:text-4xl">
              Here are some software programs I find useful, gadgets I'm
              particularly fond of, and other recommendations I have.
            </p>
          </div>
          <p class="mt-4 text-neutral-400 dark:text-neutral-300">
            People often ask me about the tools and products I use to develop
            software, stay productive, or distract myself from procrastinating.
            So, I've compiled a comprehensive list of all my favorite things.
          </p>
        </div>
        <div class="mt-12 lg:mt-24 grid grid-cols-1 lg:grid-cols-3 items-start">
          <div>
            <h3
              class="text-lg font-semibold leading-6 text-neutral-900 dark:text-white group-hover:text-blue-400">
              Workstation setup
            </h3>
          </div>
          <ul
            role="list"
            class="grid grid-cols-1 gap-6 dark:border-neutral-700 py-12 lg:py-0 lg:col-span-2">
            {
              tool.slice(0, 4).map((template) => (
                <>
                  <li class="flow-root">
                    <div class="relative flex items-center">
                      <div>
                        <h3 class="text-lg font-semibold leading-6 text-neutral-900 dark:text-white group-hover:text-blue-400">
                          <span
                            title={template.title}
                            class="focus:outline-none">
                            <span class="absolute inset-0" aria-hidden="true" />
                            <span>{template.title}</span>
                          </span>
                        </h3>
                        <p class="mt-1 text-sm leading-6 text-neutral-400 dark:text-neutral-200">
                          {template.description}
                        </p>
                      </div>
                    </div>
                  </li>
                </>
              ))
            }
          </ul>
        </div>
        <div class="mt-12 lg:mt-24 grid grid-cols-1 lg:grid-cols-3 items-start">
          <div>
            <h3
              class="text-lg font-semibold leading-6 text-neutral-900 dark:text-white group-hover:text-blue-400">
              Developer Tools
            </h3>
          </div>
          <ul
            role="list"
            class="grid grid-cols-1 gap-6 dark:border-neutral-700 py-12 lg:py-0 lg:col-span-2">
            {
              tool.slice(4, 7).map((template) => (
                <>
                  <li class="flow-root">
                    <div class="relative flex items-center">
                      <div>
                        <h3 class="text-lg font-semibold leading-6 text-neutral-900 dark:text-white group-hover:text-blue-400">
                          <span
                            title={template.title}
                            class="focus:outline-none">
                            <span class="absolute inset-0" aria-hidden="true" />
                            <span>{template.title}</span>
                          </span>
                        </h3>
                        <p class="mt-1 text-sm leading-6 text-neutral-400 dark:text-neutral-200">
                          {template.description}
                        </p>
                      </div>
                    </div>
                  </li>
                </>
              ))
            }
          </ul>
        </div>
        <div class="mt-12 lg:mt-24 grid grid-cols-1 lg:grid-cols-3 items-start">
          <div>
            <h3
              class="text-lg font-semibold leading-6 text-neutral-900 dark:text-white group-hover:text-blue-400">
              Productivity
            </h3>
          </div>
          <ul
            role="list"
            class="grid grid-cols-1 gap-6 dark:border-neutral-700 py-12 lg:py-0 lg:col-span-2">
            {
              tool.slice(7).map((template) => (
                <>
                  <li class="flow-root">
                    <div class="relative flex items-center">
                      <div>
                        <h3 class="text-lg font-semibold leading-6 text-neutral-900 dark:text-white group-hover:text-blue-400">
                          <span
                            title={template.title}
                            class="focus:outline-none">
                            <span class="absolute inset-0" aria-hidden="true" />
                            <span>{template.title}</span>
                          </span>
                        </h3>
                        <p class="mt-1 text-sm leading-6 text-neutral-400 dark:text-neutral-200">
                          {template.description}
                        </p>
                      </div>
                    </div>
                  </li>
                </>
              ))
            }
          </ul>
        </div>
      </div>
    </div>
  </div>
</section>
