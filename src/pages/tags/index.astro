---
import { getCollection } from "astro:content";
import BaseLayout from "../../layouts/BaseLayout.astro";
const allPosts = await getCollection("posts");
const tags = [...new Set(allPosts.map((post) => post.data.tags).flat())];
const pageTitle = "Tag Index";
---

<BaseLayout pageTitle={pageTitle}>
  <section>
    <div class="mx-auto max-w-3xl px-8 py-24 sm:pt-32">
      <div class="mx-auto max-w-2xl">
        <div>
          <div>
            <div>
              <p
                class="text-2xl font-medium tracking-tight text-neutral-900 dark:text-white sm:text-4xl">
                A tag slug is the text that is included in the URL of a blog
                post when a tag is used
              </p>
            </div>
            <p class="mt-4 text-neutral-400 dark:text-neutral-300">
              Tag slugs can be important for SEO, as they can help search
              engines understand the content of the blog post and improve the
              chances of the post appearing in relevant search results.
              Additionally, tag slugs can make it easier for readers to remember
              and share the URL of a specific page within the blog.
            </p>
          </div>
          <ol
            role="list"
            class="mt-12 lg:mt-24 border-t border-b dark:border-neutral-700 py-12 space-y-10 sm:space-y-16">
            <li>
              <ol role="list">
                {
                  tags.map((tag) => (
                    <li
                      class="flex justify-between py-3"
                      aria-label="Strokes and fills on page 21">
                      <span class="text-lg font-semibold leading-6 hover:text-blue-400 text-neutral-900 dark:text-white group-hover:text-blue-400">
                        <a href={`/tags/${tag}`}>{tag}</a>
                      </span>
                    </li>
                  ))
                }
              </ol>
            </li>
          </ol>
        </div>
        <div class="tags"></div>
      </div>
    </div>
  </section>
</BaseLayout>
